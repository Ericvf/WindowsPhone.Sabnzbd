<phone:PhoneApplicationPage 
    x:Class="BitSq.Wp7.SabNZBd.App.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="800" 
    d:DataContext="{d:DesignData SampleData/MainViewModelSampleData.xaml}"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait"  Orientation="Portrait"


    xmlns:uc="clr-namespace:BitSq.Wp7.SabNZBd.App.UserControls"

    shell:SystemTray.IsVisible="False">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">

        <!--Panorama control-->
        <controls:Panorama Title="SabNZBd+">
            <!--InfoPage-->
            <controls:PanoramaItem Header="Info" Name="infoPage">
                <StackPanel>
                    <uc:QueueInfo />
                    <uc:SystemInfo />
                </StackPanel>
            </controls:PanoramaItem>

            <!--QueuePage-->
            <controls:PanoramaItem Header="Queque" Name="queuePage">
                <ListBox ItemsSource="{Binding QueueItems}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <uc:QueueItem Margin="0,0,0,15" DataContext="{Binding}" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </controls:PanoramaItem>

            <!--HistoryPage-->
            <controls:PanoramaItem Header="History" Name="historyPage">
                <ListBox ItemsSource="{Binding HistoryItems}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <uc:HistoryItem Margin="0,0,0,15" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </controls:PanoramaItem>

            <controls:PanoramaItem Header="Search" />
            <controls:PanoramaItem Header="Settings">
                <ScrollViewer Margin="15,0,0,0">
                    <StackPanel>
                        <TextBlock Text="Specify SabNZBd+ server:" Foreground="{StaticResource PhoneAccentBrush}" />
                        <TextBox />

                        <TextBlock Text="Authentication type:" Foreground="{StaticResource PhoneAccentBrush}" />
                        <RadioButton Content="Username and password" Name="rbUserNameAndPassword" Checked="authenticationTypeChanged" IsChecked="true" />
                        <RadioButton Content="API key" Name="rbApiKey" Checked="authenticationTypeChanged" />

                        <StackPanel Name="spUserNameAndPassword">
                            <TextBlock Text="Username &amp; password:" Foreground="{StaticResource PhoneAccentBrush}" />
                            <TextBox />
                            <PasswordBox />
                        </StackPanel>
                        <StackPanel Name="spApiKey">
                            <TextBlock Text="API key:" Foreground="{StaticResource PhoneAccentBrush}" />
                            <TextBox />
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </controls:PanoramaItem>
        </controls:Panorama>
    </Grid>
</phone:PhoneApplicationPage>